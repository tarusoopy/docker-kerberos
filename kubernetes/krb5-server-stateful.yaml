apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  labels:
    app: krb5kdc
  name: krb5kdc
  namespace: nakano-a
spec:
  serviceName: krb5kdc
  replicas: 1
  template:
    metadata:
      labels:
        app: krb5kdc
    spec:
      containers:
      - name: krb5kdc
        image: tarusoopy/docker-kerberos
        envFrom:
        - configMapRef:
            name: krb5kdc-env
